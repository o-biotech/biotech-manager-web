import { SlideToggle, SlideToggleProps } from "@fathym/atomic";
import { IconProps, MenuIcon } from "$fathym/atomic-icons";

export default function InteractiveMenuIcon(props: IconProps) {
  return <MenuIcon {...props} />;
}
